- defaults:
    link:
      create: true
      relink: true

- clean:
    ~/:
    ~/.config:
      recursive: true

- link:
    ~/.gitconfig: gitconfig
    ~/.config/nvim: nvim
    ~/.config/alacritty/alacritty.toml: alacritty.toml
    ~/.zshrc: zshrc
