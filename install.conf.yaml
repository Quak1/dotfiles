- defaults:
    link:
      create: true
      relink: true

- clean:
    ~/:
    ~/.config:
      recursive: true

- link:
    ~/.gitconfig: gitconfig
    ~/.config/nvim: nvim
    ~/.config/fish/configh.fish: fish
    ~/.config/kitty: kitty
