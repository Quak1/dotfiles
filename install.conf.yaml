- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.vimrc: vimrc
    ~/.zshrc:
      force: true
    ~/.p10k.zsh: 
    ~/.tmux.conf:
    ~/.config/nvim: nvim
    ~/.config/nvim/lua/custom: nvchad-custom
